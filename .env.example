# Copy this file to .env and adjust values for your environment.
# The app reads these via process.env; no dotenv package required if you
# export them in your shell or systemd unit.

# URL of the legacy system being proxied (no trailing slash)
LEGACY_TARGET=http://legacy-system.local

# Port this wrapper listens on
PORT=3000
